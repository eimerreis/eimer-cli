{
  "name": "@scripts/eimer",
  "version": "0.1.3",
  "private": true,
  "type": "module",
  "description": "Meta CLI for all scripts packages",
  "author": "",
  "bin": {
    "eimer": "./dist/index.js"
  },
  "scripts": {
    "postinstall": "bunli generate",
    "dev": "bun run src/index.ts",
    "build": "bunli build --native --outfile ../../bin/eimer",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@bunli/core": "latest",
    "@scripts/config": "workspace:*",
    "@scripts/pipeline": "workspace:*",
    "@scripts/pr": "workspace:*",
    "@scripts/release": "workspace:*",
    "@scripts/task": "workspace:*",
    "@scripts/ui": "workspace:*",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@bunli/test": "latest",
    "@types/bun": "latest",
    "bunli": "latest",
    "typescript": "^5.0.0"
  },
  "bunli": {
    "entry": "./src/index.ts",
    "outDir": "./dist"
  }
}
